version: '3.8'

services:
  app-dev-ml-service-agent:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8009:8009"
    environment:
      - ENVIRONMENT=debug
    restart: on-failure
    networks:
      - webnet

networks:
  webnet:
    driver: bridge


