# ASGI server & framework
fastapi==0.115.3
uvicorn==0.29.0
gunicorn==21.2.0

# Bumped from 3.8.6 — too old for a compatibility shim in newer `openai`
# (pulled in transitively via synthscholar -> crewai -> litellm -> openai),
# which references aiohttp.SocketTimeoutError (added in a later aiohttp release).
aiohttp==3.14.3
async-timeout==4.0.3
# Logging
rich==13.9.4
python-json-logger==2.0.7
asgi-correlation-id==4.3.4
logtail-python==0.2.10

# Config & Settings
pydantic-settings==2.5.2
pkonfig==2.0.0

# Compatibility
eval-type-backport==0.1.3

# Auth & Security
python-jose==3.3.0
python-multipart==0.0.18
passlib[bcrypt]==1.7.4
# Note: bcrypt 4.x has 72-byte password limit. Code handles truncation automatically.
bcrypt==3.2.2
# Databases
psycopg==3.1.18
asyncpg==0.29.0
motor==3.3.2
pymongo==4.6.1
httpx==0.27.0
aiofiles==23.2.1

websockets==15.0.1
# Real-time communication
sse-starlette==1.8.2
cffi==1.17.1

# Weaviate and gRPC dependencies
# protobuf/grpcio* bumped from 4.25.3/1.60.2 — the old pins conflicted with
# synthscholar's transitive deps (crewai -> chromadb -> opentelemetry-exporter-otlp-proto-grpc),
# which need a protobuf runtime new enough to satisfy their generated pb2 code
# ("google.protobuf.runtime_version" gencode/runtime check). Verified
# weaviate-client==4.16.3 still imports fine against these newer versions.
weaviate-client==4.16.3
protobuf==7.36.1
grpcio==1.84.0
grpcio-tools==1.84.0
grpcio-health-checking==1.84.0

PyMuPDF==1.26.5
ollama==0.6.0

# SynthScholar (PRISMA literature review). The `synthscholar` package owns
# the AI pipeline; the local module under core/synth_scholar/ is just the
# orchestration layer (sessions, SSE, exports). SQLAlchemy 2.0 async backs
# the review tables, separate from ml_service's raw-asyncpg pool.
synthscholar[fulltext,semantic]==0.0.11
sqlalchemy[asyncio]>=2.0.30