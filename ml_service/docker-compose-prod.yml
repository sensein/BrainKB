version: '3.8'

services:
  app-prod-ml-service-agent:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8007:8007"
    environment:
      - ENVIRONMENT=production
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    driver: bridge


